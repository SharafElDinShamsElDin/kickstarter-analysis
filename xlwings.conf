# xlwings configuration file
# Place this in the project root so xlwings can find it

[Excel]
# Path to the Python module containing UDFs
# Can be an absolute path or relative to this config file
python_path = .

[UDF]
# Modules to scan for @xw.func decorators
modules = main, excel_integration

[Ribbon]
# Custom ribbon XML (optional)
